# apps

Application code for Thrive OS. Each application should live in its own subdirectory with its own README, built against an approved specification in `vault/05 Specifications/`.

No application code has been implemented yet.
